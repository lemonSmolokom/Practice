# Розв'язання диференціального рівняння (1) САР авіаційного двигуна
## Метод Рунге-Кутта 4-го порядку

---

##  Завдання

**Скласти програму для дослідження диференціального рівняння (1) методом Рунге-Кутта 4-го порядку. Для цього змінювати час t від t=0 до tₖ з кроком h. Результати занести в файл.**

---

##  Рівняння (1)

Система автоматичного регулювання (САР) авіаційного двигуна описується диференціальним рівнянням 4-го порядку:

```
T·d⁴x/dt⁴ + (1 + r·T·k₂)·d³x/dt³ + T·k₁·k₂·k₃·d²x/dt² = 
    = k₁·T·d³F/dt³ + (k₁ + r·T·k₂)·d²F/dt²
```

Де:
- **x(t)** - кількість обертів валу турбіни за секунду (вихідна величина)
- **F(t)** - зовнішнє збурення (вхідна величина)
- **T** - постійна часу системи
- **r** - коефіцієнт зворотного зв'язку
- **k₁, k₂, k₃** - коефіцієнти передачі ланок системи

---

## 🔢 Параметри системи

У програмі використані наступні параметри:

```cpp
T  = 0.1    // постійна часу
r  = 1.5    // коефіцієнт зворотного зв'язку
k1 = 2.0    // коефіцієнт передачі 1
k2 = 1.0    // коефіцієнт передачі 2
k3 = 0.5    // коефіцієнт передачі 3
```

**Збурення:** F(t) = F₀·e^(-α·t), де:
- F₀ = 10.0 (початкова подача палива)
- α = 0.3 (швидкість загасання)

**Фізичний сенс:** "Натискаємо газ на початку (F₀=10), потім поступово відпускаємо" - експоненціальне загасання, **завжди позитивне**.

---

##  Метод Рунге-Кутта 4-го порядку

### Загальний принцип

Для розв'язання диференціального рівняння виду **dy/dt = f(t, y)** метод РК-4 використовує формулу:

```
k₁ = f(t, y)
k₂ = f(t + h/2, y + h·k₁/2)
k₃ = f(t + h/2, y + h·k₂/2)
k₄ = f(t + h, y + h·k₃)

y(t+h) = y(t) + (h/6)·(k₁ + 2k₂ + 2k₃ + k₄)
```

### Чому 4-й порядок?

- **Точність:** Похибка O(h⁵) - дуже висока точність
- **Ефективність:** Оптимальне співвідношення точність/кількість обчислень
- **Стабільність:** Метод добре працює з різними типами рівнянь

### Переваги порядків:
- **1-й порядок (Ейлер):** Простий, але неточний (похибка O(h²))
- **2-й порядок:** Краще, але все ще недостатньо (похибка O(h³))
- **4-й порядок:** Золота середина (похибка O(h⁵))
- **5-й+ порядок:** Складніше, але не завжди краще

---

## 🔄 Зведення до системи 1-го порядку

Рівняння 4-го порядку зводиться до системи 4-х рівнянь 1-го порядку:

```
Вектор стану: y = [x, x', x'', x''']

Система:
dy₀/dt = y₁           (dx/dt = x')
dy₁/dt = y₂           (d(x')/dt = x'')
dy₂/dt = y₃           (d(x'')/dt = x''')
dy₃/dt = x⁽⁴⁾         (d(x''')/dt = x⁽⁴⁾)

Де x⁽⁴⁾ знаходиться з рівняння (1):
x⁽⁴⁾ = [k₁·T·F''' + (k₁ + r·T·k₂)·F'' - (1 + r·T·k₂)·x''' - T·k₁·k₂·k₃·x''] / T
```

---

##  Структура проекту

```
├── Types.h          - Визначення типів даних
├── RungeKutta.h     - Реалізація методу Рунге-Кутта 4-го порядку
├── EngineModel.h    - Модель САР двигуна (рівняння 1)
├── Main.cpp         - Головна програма
├── visualize.py     - Скрипт для візуалізації результатів
└── README.md        - Ця документація
```

---

##  Компіляція та запуск

### Компіляція:
```bash
g++ -std=c++17 -o engine_simulation Main.cpp -Wall
```

### Запуск:
```bash
./engine_simulation
```

### Візуалізація результатів:
```bash
python3 visualize.py
```

---

## Результати

Програма створює файл **simulation_results.csv** з наступними колонками:

| Колонка | Опис |
|---------|------|
| `t` | Час (секунди) |
| `x` | Кількість обертів |
| `x_d` | Перша похідна (швидкість зміни) |
| `x_dd` | Друга похідна (прискорення) |
| `x_ddd` | Третя похідна |
| `x_dddd` | Четверта похідна |
| `F` | Зовнішнє збурення |

### Параметри симуляції:
- **Інтервал часу:** [0, 10] секунд
- **Крок інтегрування:** h = 0.01 секунд
- **Кількість кроків:** 1001

---

##  Аналіз результатів

1. **Відкрийте CSV у Excel/LibreOffice** для табличного аналізу
2. **Побудуйте графіки** x(t), x'(t), x''(t) для візуального аналізу
3. **Порівняйте** поведінку системи з різними параметрами
4. **Використайте visualize.py** для автоматичного створення графіків

---

##  Особливості реалізації

###  Правильно реалізовано:

1. **Метод Рунге-Кутта 4-го порядку** - точна чисельна інтеграція
2. **Рівняння (1)** - повна модель САР з усіма параметрами
3. **Похідні збурення** - коректне обчислення F''(t) та F'''(t)
4. **Зведення до системи 1-го порядку** - стандартний підхід
5. **Запис результатів** - CSV формат для подальшого аналізу

###  Відмінності від попередньої версії:

Попередня реалізація була **НЕПРАВИЛЬНОЮ**, тому що:
- Використовувала рівняння (2): `f₀ = C₁·f₁ + C₂·f₂ + C₃·f₃`
- Це **алгебраїчне співвідношення**, а не диференціальне рівняння
- Не враховувала похідні збурення F''(t) та F'''(t)
- Не містила всіх параметрів T, r, k₁, k₂, k₃

**Поточна реалізація вирішує саме рівняння (1)** з усіма необхідними параметрами.

---

##  Додаткова інформація

### Про рівняння (2):

Рівняння (2) `f₀(t) = C₁·f₁(t) + C₂·f₂(t) + C₃·f₃(t)` - це **перепозначення** після підстановки:
- C₃ = T
- C₂ = (1 + r·T·k₂)
- C₁ = T·k₁·k₂·k₃
- f₁(t) = x''
- f₂(t) = x'''
- f₃(t) = x⁽⁴⁾

Це використовується для **діагностики САР** через функції непропорційності (розширене завдання бакалаврської роботи), але **не для розв'язання** диференціального рівняння.

---

##  Автор

Програма створена для дослідження САР авіаційного двигуна в рамках бакалаврської роботи.

**Тема:** "Інформаційна система оперативного контролю авіаційного двигуна"

---

##  Висновки

Програма успішно реалізує чисельне розв'язання диференціального рівняння (1) методом Рунге-Кутта 4-го порядку. Результати зберігаються у CSV файлі для подальшого аналізу.

Метод РК-4 забезпечує високу точність (похибка O(h⁵)) при оптимальній кількості обчислень, що робить його ідеальним вибором для інженерних задач.
